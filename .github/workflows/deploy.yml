name: Build & Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      target:
        description: 'Deploy to'
        type: choice
        default: test
        options:
          - test
          - production

jobs:
  # ==================== Build & Push Images ====================
  build:
    name: Build & Push Images
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - uses: docker/setup-buildx-action@v3

      - name: Build and push ecampus
        uses: docker/build-push-action@v6
        with:
          context: .
          file: deployments/ecampus/Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/ecampus:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/ecampus:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push ecampus-crm
        uses: docker/build-push-action@v6
        with:
          context: .
          file: deployments/ecampus-crm/Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/ecampus-crm:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/ecampus-crm:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ==================== Deploy to Test ====================
  deploy-test:
    name: Deploy to Test
    needs: build
    runs-on: ubuntu-latest
    environment: test
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        env:
          DOCKERHUB_USER: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
          APP_PROFILE: test
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          envs: DOCKERHUB_USER,DOCKERHUB_TOKEN,APP_PROFILE
          script: |
            cd /opt/ecampus && git pull
            echo $DOCKERHUB_TOKEN | docker login -u $DOCKERHUB_USER --password-stdin
            docker pull $DOCKERHUB_USER/ecampus:latest
            docker pull $DOCKERHUB_USER/ecampus-crm:latest
            cd deployments
            docker compose up -d --no-build --no-deps ecampus ecampus-crm

  # ==================== Deploy to Production ====================
  deploy-prod:
    name: Deploy to Production
    needs: [build, deploy-test]
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.target == 'production'
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        env:
          DOCKERHUB_USER: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
          APP_PROFILE: prod
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          envs: DOCKERHUB_USER,DOCKERHUB_TOKEN,APP_PROFILE
          script: |
            cd /opt/ecampus && git pull
            echo $DOCKERHUB_TOKEN | docker login -u $DOCKERHUB_USER --password-stdin
            docker pull $DOCKERHUB_USER/ecampus:latest
            docker pull $DOCKERHUB_USER/ecampus-crm:latest
            cd deployments
            docker compose up -d --no-build --no-deps ecampus ecampus-crm
